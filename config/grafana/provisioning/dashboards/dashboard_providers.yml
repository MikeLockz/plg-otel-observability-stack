apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: '' # Provision dashboards directly in the General folder (root)
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
