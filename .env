# This file contains environment variables for the observability stack.
# Docker Compose will automatically load this file.

# The IP address of the host where the application stack (e.g., el-dorado) is running.
# This is used by Prometheus to scrape metrics from exporters running on that host.
APP_STACK_HOST_IP=192.168.1.44
